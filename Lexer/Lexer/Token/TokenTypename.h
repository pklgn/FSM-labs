#pragma once

enum class TokenTypename
{
	IDENTIFIER,
	KEYWORD,
	NUMBER,
	OCTAL,
	DECIMAL,
	HEXADECIMAL,

	// arithmetic
	ADD_OPERATOR,
	SUB_OPERATOR,
	MUL_OPERATOR,
	DIV_OPERATOR,
	EXP_OPERATOR,

	ASG_OPERATOR,

	// comparison
	EQV_OPERATOR,
	NEQV_OPERATOR,
	LES_OPERATOR,
	GRT_OPERATOR,
	LES_OR_EQV_OPERATOR,
	GRT_OR_EQV_OPERATOR,

	// logical
	NOT_OPERATOR,
	AND_OPERATOR,
	OR_OPERATOR,

	// string
	CNCT_OPERATOR,

	STRING,
	DELIMITER,
	COMMENTARY,

	ERROR,
};
